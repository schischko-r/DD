# Zeroed HTML reports

- `current/` — последние исходные HTML-отчёты из предоставленной папки `test/`.
- `legacy/` — ранее лежавшие в корне репозитория zeroed-варианты.

Файлы хранятся как исходные вложения и не используются сборкой standalone-отчёта.
